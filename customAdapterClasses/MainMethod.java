package customAdapterClasses;

public class MainMethod {
    public static void main(String[] args) {
        new Inheritator();
        // Purpose of this Project ? 
        // Imagine a Frame where you want to use Multiple types of EventListener's & You have to implement multiple EventListeners.
        // But, is it possible to instead Inherit one class and use multiple EventListeners ?
    }
}
